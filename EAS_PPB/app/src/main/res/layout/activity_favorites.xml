<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white_2"
    tools:context=".activities.FavoritesActivity">

    <TextView
        android:id="@+id/tvFavorites"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="40dp"
        android:layout_marginStart="30dp"
        android:text="My Favorites"
        android:textSize="30dp"
        android:textColor="@color/light_orange"
        android:fontFamily="@font/nunito_bold"/>

    <ImageButton
        android:id="@+id/imagebutton_BackNotification"
        android:layout_width="29dp"
        android:layout_height="29dp"
        android:layout_marginTop="10dp"
        android:layout_marginStart="10dp"
        android:layout_gravity="top|left"
        android:background="@null"
        android:src="@drawable/ic_left_arrow_light_orange"/>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerview_FavList"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginTop="125dp"
        android:layout_marginEnd="20dp"
        android:layout_marginStart="20dp"
        android:paddingBottom="50dp"
        android:clipToPadding="false"
        tools:listitem="@layout/cardview_menu"/>

</FrameLayout>